Information About the Post

<%= render partial: "authors/author", locals: {post_author: @post.author} %>
<%# We can use locals (above) to make sure @authors is referenced to in the authors/_author html form %>
<%# render 'authors/author' %>

<%= @post.title %>
<%= @post.content %>
